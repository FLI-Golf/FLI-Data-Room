# ── PocketBase (server-only, never expose to frontend) ──────────────────────
POCKETBASE_ADMIN_EMAIL=email
POCKETBASE_ADMIN_PASSWORD=update

# ── Frontend (safe to expose — PUBLIC_ prefix required by SvelteKit) ─────────
PUBLIC_POCKETBASE_URL=https://pocketbase-rxik-production.up.railway.app
